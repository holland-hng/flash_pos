name: core_router
description: Core router


publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  auto_route: ^7.1.0

  core_dependency:
    path: '../core_dependency'

dev_dependencies:

  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner:
  injectable_generator:

flutter:
  uses-material-design: true

